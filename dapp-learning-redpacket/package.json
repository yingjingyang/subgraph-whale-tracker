{
  "name": "dapp-learning-redpacket",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ dapp-learning-redpacket",
    "create-local": "graph create --node http://localhost:8020/ dapp-learning-redpacket",
    "remove-local": "graph remove --node http://localhost:8020/ dapp-learning-redpacket",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dapp-learning-redpacket",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
